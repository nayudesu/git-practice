<h2>タイトル</h2>
<p><%= @list.title %></p>

<h2>本文</h2>
<p><%= @list.body %></p>

<h2>画像</h2>
 <% if @list.image.attached? %>
  <%= image_tag @list.image, size: "200x200" %>
 <% else %>
  <%= image_tag 'no_image', size: "200x200" %>
 <% end %>

<%= link_to "編集", url: edit_list_path(@list.id) %> <!--詳細画面に編集画面へのリンクを追加(名前付ルートで）-->
<%= link_to "削除", list_path(@list.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>
<!--data-confirm＝削除確認ダイアログも表示されるように設定する-->